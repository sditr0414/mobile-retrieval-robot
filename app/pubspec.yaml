name: mobile_retrieval_robot_app
description: 모바일 물품 회수 로봇 제어 앱
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_classic_bluetooth: ^0.1.5
  flutter_joystick: ^0.2.2
  provider: ^6.1.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
